#!/bin/sh
set +e
message=${*:-}
mkdir -p /run /var/log
printf '%s %s\n' "$(date -u '+%H:%M:%S')" "$message" >> /var/log/boot-progress.log
for console in /dev/tty0 /dev/ttyS0; do
  test -c "$console" && printf '[sc-adat] %s\n' "$message" > "$console" 2>/dev/null
done
