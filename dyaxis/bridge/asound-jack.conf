# Make the normal ALSA default feed the running JACK server.
pcm.!default {
  type jack
  playback_ports {
    0 system:playback_1
    1 system:playback_2
  }
}

ctl.!default {
  type hw
  card ADAT
}
