################################################################################
#
# supercollider-headless
#
################################################################################

# Intentionally incomplete. Pin the source release and verified hashes only after
# validating a headless native Debian build and the Buildroot dependency graph.
